# Built binaries
/bin/

# Local env / secrets
.env

# OS / editor cruft
.DS_Store

# Claude Code local scratch (never commit)
.claude/

# Rust build artifacts — source (incl. guest-rs/.cargo/config.toml) is committed.
target/
Cargo.lock

# NOTE: agent/quickjs_guest.wasm is intentionally COMMITTED — it's a prebuilt
# artifact (see `make guest-rs`) so `go build` works without the Rust toolchain.
# Do NOT ignore it.
